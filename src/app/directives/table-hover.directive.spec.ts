import { TableHoverDirective } from './table-hover.directive';

describe('TableHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new TableHoverDirective();
    expect(directive).toBeTruthy();
  });
});
