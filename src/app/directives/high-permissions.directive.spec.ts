import { HighPermissionsDirective } from './high-permissions.directive';

describe('HighPermissionsDirective', () => {
  it('should create an instance', () => {
    const directive = new HighPermissionsDirective();
    expect(directive).toBeTruthy();
  });
});
